The Best AI Course


Neural Networks:

    

    screenPlay:

        SHOW: 
            reveal lots of differently written 7s in MNIST, 
        TELL:
            your brain recognizes all of these as 7s, but how?
            none of them are exactly alike, all different, but
            somehow the same!


        SHOW: 
            image pixels into colored cubes, each valued 0-255
        TELL:
            here is the problem you need to solve:
            a banking company has hired you to solve a problem 
            they've been having, each day there are millions of cheques 
            & hand-written documents that come in, & their information
            needs to be fed into the computers, costumers are unhappy because 
            the work is slow! the bank employees cannot enter the hand-written 
            information into the computers fast enough!

            solve this problem & you will get paid lots of money!


        SHOW:
            xxx
        TELL:
            the crucial thing about NNs you need to underst& is this:
            they learn representations of the dataset,

            in one of our later projects we will train a NN to develop a 
            limited understanding of english words, you will see that it
            learns to put each word in a special position in 3D space that 
            as has neighboring points which means similar things, & it 
            learns that moving in some directions of this space, makes the 
            word to have more of a certain quality, for example one direction
            makes the word more feminine or more musculine, other directions 
            make the world more polite or rude, singular or plurar!





        



        SHOW:
            perceptron, shallow, deep, line, curve, fold
        TELL: 
            we will start with the simplest possible thing, a single neuron,
            we will see what can it do, we will play with it & discover its 
            abilities & limits...

            then we put a few of these neurons in a layer, now we make them
            work with each other, when they work collectively, they become 
            more powerful & can solve harder problems, we will play with 
            them & push them to their limits as well...

            then we reach the boss!
            we stack multiple layers of these neurons next to each other,
            the outputs of one layer becomes the inputs of the next layer!
            these things become super-powerful ! one layer can learn to 
            solve part of the problem & let other layers take care of the 
            rest, or learn which parts of the signal to extract & what parts
            to ignore & not pass on so to make the job of the next layer 
            easier!

            Actually, a relatively small multi-layer perceptron, can have more
            linear regions than the number of atoms in the obsurvable universe!


        SHOW:
            fitting the sine curve by changing the linear regions
        TELL: 
            a neuron is a simple function, a NN is a very complicated function
            made of simple functions



        SHOW:

        TELL:
            you feed-in the input, it passes through the model & its 
            parameters make changes to it, then it gives you an output, what
            it thinks the right answer should be!

            but it is wrong! 
            the parameters are not trained to learn the right behavior, so the
            changes they make are useless this makes the output trash!

            but even the greatest masters gotta start from somewhere:
            every possible setting of parameters that a NN can possibly have, 
            form a space, a space of NNs, each NN in this space has a different 
            behavior becasue its parameters are set differently, we want to find
            the NN that behaves best on our problem, how do we find it?

            lets imagine we had a function that took our NN's parameters as input
            & gave us a number back as output, this number tells us how well 
            our NN performs the task we want it to do, lets call it the 
            "loss function", becasue if the number we get as output is high, it means
            our NN behaves badly which leads to a large loss, the function asks our 
            NN questions & more wrong answers it gives, more money we lose, 

            now that we have a way of telling a good NN from a bad NN, how do we find
            the best NN possible?

            you can just initialize parameters at random & l& at different places 
            in this loss landscape, but it is extremely unlikely that a random 
            initialization lands on a point of low loss(that is, a well behaving NN)
            so...
            we need a way to navigate towards lower loss points


        SHOW:
        TELL:
            Supervised Learning:
                Question: How can a model learn a mapping from inputs to known 
                          correct outputs?
                Issue: The model needs labeled data, & performance depends 
                       heavily on label quality & coverage.
                Example: the input might be the age & mileage of a second-h& car, 
                         & the output might be the price in dollars.

            Self-Supervised Learning:
                Question: How can a model learn useful representations from 
                          unlabeled data by creating its own training signal?
                Issue: There are no human-provided labels, so the model must 
                       define proxy tasks that force it to learn meaningful 
                       structure.
                Example: Masking words in a sentence & training a model to 
                         predict the missing words.
            
            Un-Supervised Learning:
                Question: How can a model discover structure in data without labels?
                Issue: There is no explicit target output, so the model must infer 
                       patterns such as clusters, latent factors, or compressed 
                       representations.
                Example: Grouping customers into segments based only on purchase 
                         behavior.

            Reinforcement Learning:
                Question: How can an agent learn what to do by interacting with an 
                          environment & receiving rewards?
                Issue: Feedback is delayed, sparse, & depends on sequences of 
                       actions rather than single predictions.
                Example: Learning to play a game by trying actions & improving 
                         based on wins & losses.



        SHOW:
        TELL:

        SHOW:
        TELL:
            so to calculate the error, to tell the NN how wrong it was, we get our 
            labes for this example, & see how different the NN's answer is from
            this example's labels:

            the simplest way is to just subtract the NN's answer from the labels,
            but that way if one error is too negative & another error is too 
            positive, they cancel out to give no error! when they should've given 
            double their individual errors! being in two different ways doesn't 
            cancel out to being right! 

            so when we subtract the the answer & label, we square the result!
            this way all the errors become positive so to not cancel-out each other.

            Alright, we subtract the NN's predictions from the labels, then square the 
            differences, now we have 10 positive numbers each telling us how wrong
            the NN is in one way or another, we want a single number for our loss 
            landscape, so we take an average of these 10 losses, sum them up & 
            divide by how many there are. 
            the number we get is the NN's loss calculated for a single example.

            This way of calculating loss is called "The Mean Squared Error"

        SHOW:
        TELL:

        SHOW:
        TELL:

        SHOW:
        TELL:
            Here goes the NN Training loop:

            see where you are -> calc loss for NN
            see where to go   -> canculate the gradient vector
            go there          -> update parameters 



    Q4AI:

        How to install the python programming language on my system?
        
        Teach me the basics of python programming concepts with intuitive & 
        hands-on examples

        What is the Pytorch library, how to install it on my system & check
        if it is successfully installed




    Preview:

        The goal of this course is to help you deeply understand one of the most important technologies of our time:
        artificial intelligence.

        We will begin with the simplest possible building block—a single artificial neuron. You will learn how it works, 
        how to build it from scratch, and how to train it to solve meaningful problems.

        From there, we will increase the complexity step by step. First, we will place several neurons side by side 
        to create a shallow neural network. You will see what new capabilities emerge from this simple expansion, and 
        what kinds of problems such a network can solve.

        Finally, we will stack multiple layers of neurons on top of one another to build a deep neural network. This 
        powerful structure forms the foundation of modern AI systems. With enough data and training, it can learn 
        remarkably complex patterns and tackle many of the problems we care about today.

        To demonstrate its power, we will train these networks to solve real challenges, including:

        Classifying images of handwritten digits
        Learning how to balance a pole by moving the cart beneath it
        Transforming randomly scattered points into any shape we train it on
        And ultimately, generating entirely new images of handwritten digits

        By the end of this course, you will not only understand how these systems work—you will have built them yourself, 
        step by step.



    Why Neural Networks?
        There are thousands of different techniques for making computers behave intelligently and learn 
        from data. Over the years, researchers have explored rule‑based systems, probabilistic models, 
        symbolic reasoning, evolutionary algorithms, and many other approaches.

        Yet among all of these methods, neural networks have proven to be the most broadly successful.

        The reason is surprisingly simple. With enough data and enough compute, a neural network of sufficient 
        size can approximate almost any function. In other words, many clever algorithms that humans design can, 
        in principle, be learned and reproduced by a neural network. Taken to the extreme, a neural network could 
        even learn to imitate the thinking process of the person who invented that algorithm in the first place.

        Now imagine pushing this idea even further.

        Picture a future where most software is implemented not as hand‑written programs, but as trained neural 
        networks. Your phone’s operating system, the apps you use, and even the logic behind websites could all 
        be neural networks. They would take inputs such as touch, voice, images, and internet data—and transform 
        them into outputs like pixels on your screen, sounds from your speakers, or vibrations from the device.

        Why might software evolve in this direction?

        Because both consumers and businesses value efficiency. We constantly try to build systems that do more 
        while consuming fewer resources. Traditionally, achieving efficiency required a human programmer to deeply 
        understand a problem and carefully design the algorithm that maps inputs to outputs. The quality of the 
        software was therefore limited by how clever that programmer could be.

        Neural networks change this dynamic.

        Instead of manually designing the algorithm, we simply provide examples of inputs and the outputs we want. 
        During training, the network gradually discovers an internal process that performs the task. Through millions 
        or billions of small adjustments to its parameters, it effectively searches for an efficient algorithm on its own.

        This leads to a famous—and slightly unsettling—observation in machine learning:

        “Gradient descent is the world’s best programmer.”

        Rather than writing programs line by line, we increasingly allow optimization and learning to discover 
        the program for us.
                
 ____   
        


    The Perceptron:
        This is the simplest building block of modern artificial intelligence. Its design was inspired by 
        how neurons work in the human brain. A biological neuron receives signals from many sources—such as 
        sensory organs or other neurons. Some of these signals are strengthened, while others are weakened. 
        The adjusted signals are then combined inside the neuron.

        Each neuron also has a built‑in tendency that determines how easily it becomes active; in artificial 
        models this tendency is called the bias. After the signals are combined and adjusted by this bias, the 
        result is passed through an activation function, which determines the neuron’s final output and allows 
        it to produce interesting, nonlinear behavior.

        This simple computational model is known as the perceptron—a name chosen to reflect the idea that it is 
        a small machine capable of a primitive form of perception.



        Key idea:
            Weights: scale inputs (amplify/dampen)/determine how strong each input signal is
            Bias: shifts the activation threshold/determines how easily the neuron fires 
                  (baseline excitability).

            “These weights decide how loud each voice is.”
            “Bias decides how easy it is to wake the neuron up.”

    Shallow Neural Networks:


        Loss landscape:

            Each entry in this parameter tensor can vary independently of every other entry. Mathematically, this means 
            every single parameter represents its own unique dimension. Consequently, if we wanted to fully visualize the 
            space of every possible configuration, we would often need a landscape with thousands—or even trillions—of 
            dimensions.

            Since our brains can’t visualize such a complex space, we use 2D and 3D diagrams to build our intuition. 
            These simplified models help us understand the “topography” of the loss landscape. Once we have that mental 
            picture, we use calculus and linear algebra to apply those same concepts to the much higher-dimensional spaces 
            where real neural networks live.





    Deep Neural Networks:

        "A system built from very simple parts can imitate astonishingly complicated behavior"

        Tensors All the Way Down
            In modern neural networks, everything is represented as a tensor.

            A tensor of rank 0 is simply a single number—a scalar.
            A tensor of rank 1 is a vector—a list of numbers arranged in a row or column.
            A tensor of rank 2 is a matrix, with a certain height and width.
            A tensor of rank 3 or higher extends this idea into additional dimensions—essentially a structured 
            collection of numbers organized along multiple axes.
            
            No matter what we are working with in a neural network, it can be expressed as a tensor:

            The inputs to the network are tensors, representing data from the outside world—such as images, text, 
            or sensor readings.
            The parameters (weights and biases) are tensors, determining how inputs are transformed.
            The gradients are tensors, telling us how to adjust those parameters to reduce error.
            Even intermediate activations flowing through the network are tensors.
            This is why tensors are so central to deep learning. They provide a single, consistent mathematical 
            language for representing data, parameters, and learning signals.

            It is also why GPUs are so effective for neural networks. GPUs are designed to perform fast, large-scale 
            operations on arrays of numbers—that is, on tensors. When we train neural networks, we are essentially 
            performing massive tensor computations in parallel. The hardware and the mathematics align perfectly.



    MNIST Classification:





        Distributed Learning
            So far, we have learned how to train a neural network on a single device. Now let’s look at one of the 
            most powerful abilities of artificial learning systems: they can learn together across many machines at once.

            A neural network can be copied onto multiple devices—different GPUs, computers, or even machines in 
            different data centers. Each copy of the network is given a different portion of the dataset and learns 
            from it independently. Periodically, these copies synchronize with one another, combining what they have 
            learned into a shared set of parameters.

            The result is a system that is no longer limited by the compute power or data access of a single machine. 
            Instead, it can scale across many devices, learning faster and from far larger datasets. In a sense, the 
            model can learn in many places at the same time—something biological learners cannot do.


            The first time I heard about distributed learning, my imagination ran wild.
            I pictured a brilliant—but slightly eccentric—scientist who digitized his own mind and placed copies of 
            it into many different human bodies: a president, a schoolteacher, a mechanic, a little girl in a classroom—half 
            the town, in fact. Outwardly, they appeared to be different people, living different lives, at different ages 
            and in different roles. But inwardly, they all shared the same underlying mind.
            Each day, every version of him would experience the world from a unique perspective. And each night, their 
            memories would synchronize. All of the day’s experiences—joys, frustrations, misunderstandings, discoveries—would 
            merge into a single, unified consciousness.
            Over time, that shared mind would grow not just in knowledge, but in empathy. It would begin to see the hidden 
            chains of cause and effect that connect people’s lives. It would understand how the cry of a little girl might 
            unsettle her mechanic father, how his distraction could lead to a mistake while repairing a president’s car, and 
            how that small event might ripple outward to influence global politics.
            Of course, this is only a thought experiment—but it captures the essence of distributed learning. Multiple instances explore 
            different parts of the world in parallel, and when they synchronize, the combined system becomes wiser than any single 
            instance could have been alone.

            To demonstrate this idea, we will train our multilayer perceptron (MLP) on the MNIST dataset using distributed 
            training.


            Here is the basic process:

            First, we create several identical copies of the same neural network. Each copy starts with exactly the 
            same parameters.

            Next, we split the training data into different mini-batches and distribute them across the devices. Each 
            device performs a forward pass and backpropagation on its own batch, computing the gradients of the loss 
            with respect to the model’s parameters.

            At this point, every device has learned something slightly different from its portion of the data. To 
            combine these insights, the devices communicate with each other and aggregate their gradients, typically 
            by averaging them.

            Once the gradients are combined, the shared parameters are updated. Every copy of the network receives 
            this updated set of parameters, keeping all models synchronized.

            Then the process repeats: each device processes a new batch of data, computes gradients, shares them, and 
            updates the parameters again.

            By repeating this cycle thousands of times, the network gradually learns from the entire dataset—while using 
            the combined computational power of many machines.

            This technique, often called data parallel training, is the backbone of modern large‑scale AI systems. It 
            is what allows neural networks to grow from small models trained on a single computer to massive systems 
            trained across thousands of GPUs.








    Love Field:

    CartPole:

    VAE: